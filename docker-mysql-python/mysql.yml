# change the properties below as per your environment
connection:
  #host: '127.0.0.1'
  host: 'host.docker.internal'  # connecting MySQL server of the host machine 
  port: '3306'
  database: 'world' 
  user: 'root'
  password: 'admin'
  raise_on_warnings: True

# host: 'host.docker.internal' instead of '127.0.0.1' when running service within docker for desktop
#see host  https://stackoverflow.com/questions/24319662/from-inside-of-a-docker-container-how-do-i-connect-to-the-localhost-of-the-mach
